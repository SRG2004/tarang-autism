# ⚠️ SECURITY WARNING: NEVER COMMIT THIS FILE TO GIT ⚠️
# Copy this file to .env and fill in your actual values
# Each service should have its own strong, randomly generated secrets

# =============================================================================
# JWT & ENCRYPTION SECRETS
# =============================================================================
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_THIS_TO_RANDOM_64_CHAR_STRING
JWT_SECRET=CHANGE_THIS_TO_ANOTHER_RANDOM_64_CHAR_STRING

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# For local development with docker-compose:
DB_USER=tarang_admin
DB_PASSWORD=CHANGE_THIS_DB_PASSWORD
DB_NAME=tarang_production
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}

# For production (Neon or other hosted PostgreSQL):
# DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================
# For local development with docker-compose:
REDIS_PASSWORD=CHANGE_THIS_REDIS_PASSWORD
REDIS_URL=redis://:${REDIS_PASSWORD}@redis:6379/0

# For production (Upstash or other hosted Redis):
# REDIS_URL=redis://default:password@host:port

# =============================================================================
# API KEYS (Optional)
# =============================================================================
GEMINI_API_KEY=

# =============================================================================
# NETWORKING
# =============================================================================
# For local development:
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
NEXT_PUBLIC_API_URL=http://localhost:8000

# For production, set these in your deployment platform:
# ALLOWED_ORIGINS=https://your-frontend-domain.com
# NEXT_PUBLIC_API_URL=https://your-api-domain.com
