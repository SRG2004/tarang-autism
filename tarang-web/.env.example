# ⚠️ TARANG Frontend Environment Configuration ⚠️
# Copy this file to .env.local for local development

# =============================================================================
# API CONFIGURATION (REQUIRED)
# =============================================================================
# Backend API URL - MUST match your backend deployment

# Local development (when running both frontend and backend locally):
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production deployment:
# NEXT_PUBLIC_API_URL=https://your-api-domain.onrender.com

# =============================================================================
# NOTES
# =============================================================================
# - All variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Never put secrets in NEXT_PUBLIC_ variables
# - For production, set this in your deployment platform (Vercel, Netlify, etc.)
# - DO NOT include trailing slashes in URLs
